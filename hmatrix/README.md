# hmatrix performance benchmarks

## run benchmarks

```
$ stack build && stack exec hmatrix-benchmarks
```

### generate report

```
$ stack build && stack exec hmatrix-benchmarks -- --output hmatrix.html
```
